import styled from 'styled-components';

const CardContainer = styled.div`
    width: 100%;
    border-radius: 25px;
    text-align: left;
`

const CardContent = styled.div`
`

export default {
    CardContainer,
    CardContent
}